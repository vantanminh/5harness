{
  "name": "@5harness/firebase",
  "private": true,
  "description": "Firebase Auth and Firestore rules for Harness Cloud",
  "scripts": {
    "emulators": "firebase emulators:start --config firebase.json",
    "deploy": "firebase deploy --config firebase.json --only auth,firestore:rules,firestore:indexes",
    "deploy:auth-rules": "firebase deploy --config firebase.json --only auth,firestore:rules"
  }
}
