{
  "name": "@5harness/firebase",
  "private": true,
  "description": "Firebase deployment commands for Harness Cloud",
  "scripts": {
    "emulators": "firebase emulators:start --config firebase.json",
    "deploy": "firebase deploy --config firebase.json --only functions,firestore:rules,firestore:indexes"
  }
}
