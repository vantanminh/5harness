---
contract: "CLI login uses a short-lived device code and browser verification, then polls the hosted Worker for rotating credentials without requiring a loopback callback URL; existing OAuth security boundaries remain enforced."
created_at: "2026-09-16T10:28:59.211696019+00:00"
e2e: 1
evidence: "Device-code login implemented in Rust CLI, Cloudflare Worker, and SPA. cargo test --all-targets; npm --prefix web run check; npm --prefix web test; npm --prefix web run test:worker; npm run release:check all pass. Worker version 97014bd8-5562-440c-b0bd-2dc0807e90d1 and Pages deployment verified: OAuth metadata advertises device_authorization_endpoint and device grant, /device serves HTML, issuance returns a bounded code, first poll returns authorization_pending. Actual Firebase approval remains a human browser action; platform-specific Windows proof not run."
id: US-105
integration: 1
lane: high_risk
links:
  - IN-033
  - US-045
  - US-101
  - 0019-mcp-oauth-21-pkce
notes: "Implement RFC 8628-style device authorization between the Rust CLI and Cloudflare Worker. Display verification URL plus user code, poll with bounded interval/expiry, preserve PKCE or an equivalent proof binding, and keep callback flow only as an explicit compatibility path if required. Never log credentials or device secrets."
platform: 0
status: implemented
title: Device-code login without loopback callback
type: story
unit: 1
updated_at: "2026-09-16T10:56:46.514487455+00:00"
verify: "cargo test --all-targets && npm --prefix web run check && npm --prefix web test && npm --prefix web run test:worker"
---

# Device-code login without loopback callback
