# Harness Maturity

How this product progresses from static docs to an operable agent harness.

## Ladder (summary)

| Level | Intent | Status |
| --- | --- | --- |
| H1 | Policy docs and agent entrypoints | Achieved |
| H2 | Durable records + CLI write/query | Achieved |
| H3 | Trace scoring + friction loops | Partial / achieved core |
| H4 | Mechanical story verification | Achieved |
| H5 | Audit + propose improvements | Achieved |
| H6 | Multi-project discovery + dashboard | Achieved (foundation) |

## Evidence (this product)

- Global npm CLI: `@vantanminh/harness` (`harness` bin).
- Markdown entity store + agent index (`reindex` / `get` / `search` / `links`).
- Quality: `story verify`, `trace`, `audit`, `propose`.
- Registry: `link` / `unlink` / `projects` + `dashboard`.

Use `harness query matrix` and story packets for proof tracking. Update this
file when a maturity claim changes.
