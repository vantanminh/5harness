# Runtime values for wrangler dev. Keep this file as a template and copy it
# to .dev.vars; never commit the copied file.
FIREBASE_PROJECT_ID=harness5
FIREBASE_API_KEY=replace-me
FIRESTORE_API_BASE_URL=http://127.0.0.1:8080
CORS_ORIGINS=http://localhost:5173,https://5harness-cloud.pages.dev
RATE_LIMIT_SALT=replace-with-at-least-32-random-characters
